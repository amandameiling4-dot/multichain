# =============================================================================
# MultiChain — Environment Variables
# Copy this file to .env.local for local development.
# In production, set these variables in Vercel's Environment Variables settings.
#
# ⚠️  NEVER commit real credentials to version control.
#     Keep .env.local in .gitignore (it is already excluded by default).
# =============================================================================

# ── Neon Postgres (Prisma) ────────────────────────────────────────────────────
# Pooled connection URL — used by Prisma Client at runtime (via Neon PgBouncer).
# Copy the "Pooled connection" string from your Neon project's Connection Details panel.
# Note: pgbouncer=true is NOT needed here because this project uses the
# @prisma/adapter-pg driver adapter (Prisma v7), which manages connections via pg directly.
DATABASE_URL="postgresql://neondb_owner:YOUR_NEON_PASSWORD@ep-billowing-recipe-aidzgwaa-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Direct (unpooled) connection URL — used only for Prisma migrations.
# Copy the "Direct connection" string from your Neon project's Connection Details panel.
DATABASE_URL_UNPOOLED="postgresql://neondb_owner:YOUR_NEON_PASSWORD@ep-billowing-recipe-aidzgwaa.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require"

# ── Admin API ─────────────────────────────────────────────────────────────────
# Secret key for admin API endpoints. Generate with: openssl rand -base64 32
ADMIN_API_KEY="replace-with-a-secure-random-string"

# ── Session ───────────────────────────────────────────────────────────────────
# Secret used to sign session tokens. Generate with: openssl rand -base64 32
SESSION_SECRET="replace-with-a-secure-random-string"

# ── Next.js ───────────────────────────────────────────────────────────────────
# Public app URL (used for redirects / CORS).
# Local development:
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Production example (set this to your Vercel deployment URL):
# NEXT_PUBLIC_APP_URL="https://your-project.vercel.app"
